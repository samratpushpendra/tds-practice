name: Test Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: windows

    steps:
      - uses: actions/checkout@v4

      - name: 23f2002573@ds.study.iitm.ac.in
        run: echo "Hello, world!"
